{
  "name": "dokb-os",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc && cp -r public dist/"
  },
  "dependencies": {
    "@vercel/node": "^3.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}
